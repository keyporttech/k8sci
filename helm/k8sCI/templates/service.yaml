apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8sCI.fullname" . }}
  labels:
    {{- include "k8sCI.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8sCI.selectorLabels" . | nindent 4 }}
